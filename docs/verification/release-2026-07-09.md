# Skill Port CLI Release Verification

Date: 2026-07-09

## Verified

| Gate | macOS | Linux |
| --- | --- | --- |
| Node.js runtime | 24.15.0 | 24.15.0, Debian Bookworm |
| Unit and integration tests | 51 passed | 51 passed |
| TypeScript typecheck | Passed | Passed |
| ESLint | Passed | Passed |
| Agent target contracts | 8 passed | 8 passed |
| Packed npm installation | Passed | Passed |

Additional checks:

- `npm ci` completed from `package-lock.json`.
- Runtime dependency audit reported zero vulnerabilities.
- `skill-port-cli` was unregistered in the npm registry when checked.
- Package inspection confirmed `dist/cli.js` and `docs/supported-targets.md` are included while source and test directories are excluded.
- The installed tarball completed the full `init -> install -> list/info -> enable -> disable -> remove` loop on macOS and Linux.
- The named platform gate passed 22 checks on macOS and Linux, including simulated Windows permission fallback behavior; native Windows evidence remains pending.
- A real managed Codex entry was created at the documented global target, explicitly invoked by Codex 0.142.5 in an isolated read-only session, and returned the fixture's exact `SKILL_PORT_DISCOVERY_OK` response.
- The Codex entry was removed through `sklp disable`; a filesystem check confirmed no managed entry remained.
- A real managed Pi entry was explicitly invoked by Pi 0.80.3 in an isolated session and returned the same exact `SKILL_PORT_DISCOVERY_OK` response.
- The Pi entry was removed through `sklp disable`; a filesystem check confirmed no managed entry remained.

## Pending External Evidence

- Windows native CI and NTFS junction behavior.
- Runtime Skill loading by the six remaining advertised Agents. Codex and Pi runtime loading passed; target contracts and entry visibility passed for all eight targets.
- GitHub Actions execution. Local `gh` authentication is now valid, but the workflows have not run remotely yet.
